// hero.ts
import { heroui } from "@heroui/react";
import plugin from "eslint-plugin-react-hooks";
export default heroui({
  themes: {
    light: {
      colors: {
        // primary: "#5e5fc4",
      },
    },
  },
});
